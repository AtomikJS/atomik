{
  "name": "atomikjs-monorepo",
  "private": true,
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev"
  },
  "dependencies": {
    "@atomikjs/core": "^0.1.0",
    "@atomikjs/http": "^0.1.0",
    "http": "^0.0.1-security"
  }
}
